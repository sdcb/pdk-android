name: Build

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/build.yml"
      - "app/**"
      - "core/**"
      - "gradle/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradlew"
      - "gradlew.bat"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Android release APK (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: x64
            runner: ubuntu-24.04
            artifact: pdk-android-release-x64
          - name: arm64
            runner: ubuntu-24.04
            artifact: pdk-android-release-arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build release APK
        run: gradle --no-daemon :app:assembleRelease

      - name: Upload release APK
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error
